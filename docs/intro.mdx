---
sidebar_position: 1
tags:
  - test
---

import Mermaid from '@theme/Mermaid';

# gitGraph

<Mermaid
  value={`gitGraph
   commit
   commit
   branch develop
   checkout develop
   commit
   commit
   checkout main
   merge develop
   commit
   commit`}
/>

# Tutorial Intro

<Mermaid
  value={`sequenceDiagram
autonumber
box Yellow Git rustore-help
participant DS-master
participant DS-branch
end
box Green Git rustore-help-tm
participant TM-master
participant TM-branch
end
box Orange local
participant DS
participant TM-source
participant TM-project
participant TM-target
end
rect rgb(0, 128, 128)
note right of DS-master: TRANSLATION TASK
DS-master->>DS-branch: branch [TASK Num]
TM-master->>TM-branch: branch [TASK Num]
end
rect rgb(0, 128, 128)
note right of DS-branch: COMBO
TM-branch->>TM-project: pull
DS-branch->>DS: pull
end
DS->>TM-source: to source
TM-source->>TM-project: t10n
TM-project->>TM-target: create target
TM-target->>DS: to target
DS->>DS: build
rect rgb(255, 0, 0)
note right of DS: ERROR
TM-project->>TM-project: fix
TM-project->>TM-target: create target
TM-target->>DS: to target
DS->>DS: build
end
rect rgb(0, 128, 128)
note right of DS-branch: COMBO
TM-project->>TM-branch: add, commit, push
DS->>DS-branch: add, commit, push
end
par
DS-master->>DS-branch: rebase
DS-branch->>DS-master: merge
and
TM-master->>TM-branch: rebase
TM-branch->>TM-master: merge
end
`}
/>

Let's discover **Docusaurus in less than 5 minutes**.

## Getting Started

Get started by **creating a new site**.

Or **try Docusaurus immediately** with **[docusaurus.new](https://docusaurus.new)**.

### What you'll need

- [Node.js](https://nodejs.org/en/download/) version 14 or above:
  - When installing Node.js, you are recommended to check all checkboxes related to dependencies.

## Generate a new site

Generate a new Docusaurus site using the **classic template**.

The classic template will automatically be added to your project after you run the command:

```bash
npm init docusaurus@latest my-website classic
```

You can type this command into Command Prompt, Powershell, Terminal, or any other integrated terminal of your code editor.

The command also installs all necessary dependencies you need to run Docusaurus.

## Start your site

Run the development server:

```bash
cd my-website
npm run start
```

The `cd` command changes the directory you're working with. In order to work with your newly created Docusaurus site, you'll need to navigate the terminal there.

The `npm run start` command builds your website locally and serves it through a development server, ready for you to view at http://localhost:3000/.

Open `docs/intro.md` (this page) and edit some lines: the site **reloads automatically** and displays your changes.
