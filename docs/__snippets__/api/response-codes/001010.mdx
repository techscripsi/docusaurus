<tr>
<td>`001010`</td>
<td>`200`</td>
<td>`Timer already paused, items [...]`</td>
<td>

The request contains an instruction to pause a timer for an item or items whose timer is already in the `paused` state. This is not an error. The timer status will remain unchanged. Paused timers can be resumed. ADDLINK

</td>
</tr>