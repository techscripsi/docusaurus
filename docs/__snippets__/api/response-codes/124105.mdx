<tr>
<td>`124105`</td>
<td><code>400</code></td>
<td>`Cannot pause an expired timer, item(s): [...]`</td>
<td>

The timer your are trying to pause is in the `expired`. Only timers in the `started` state can be paused.

:::note
An `expired` timers... TOPONDER ADDLINK
:::

</td>
</tr>