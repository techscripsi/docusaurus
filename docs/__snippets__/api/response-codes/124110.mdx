<tr>
<td>`124110`</td>
<td><code>400</code></td>
<td>`Cannot stop an expired timer, item(s): [...]`</td>
<td>


The timer your are trying to stop is in `expired` state. Only `started`, `paused`, or `pending` timers can be `stopped`.

:::note
The `expired` state of a timer is the end of its lifecycle and equal to. ADDLINK
:::

??? error?

</td>
</tr>