<tr>
<td>`124106`</td>
<td><code>400</code></td>
<td>`Cannot pause a stopped timer, item(s): [...]`</td>
<td>

The timer your are trying to pause is in `stopped` state. It has never been started. Only timers in the `started` state can be paused.

:::note
`pending` timers can be started TOPONDER. ADDLINK
:::

</td>
</tr>