<tr>
<td>`001008`</td>
<td>`200`</td>
<td>`Timer already started, item(s): [...]`</td>
<td>The request contains an instruction to start a timer for an item or items whose timer is already in the `started` state. The timer status will remain unchanged. This is not an error.</td>
</tr>